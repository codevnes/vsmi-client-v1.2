server {
    listen 80;
    server_name beta.vsmi.vn;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name beta.vsmi.vn;

    # SSL Configuration
    ssl_certificate /etc/letsencrypt/live/beta.vsmi.vn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/beta.vsmi.vn/privkey.pem;
    
    # Changed these SSL parameters to avoid conflicts
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_session_cache shared:BetaSSL:10m;
    ssl_session_timeout 1d;
    ssl_session_tickets off;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    # Proxy settings
    location / {
        proxy_pass http://localhost:3031;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
} 